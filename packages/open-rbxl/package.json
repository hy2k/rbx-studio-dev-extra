{
	"name": "open-rbxl",
	"description": "Open rbxl/rbxlx place file in Roblox Studio",
	"version": "0.0.1",
	"type": "module",
	"main": "dist/index.js",
	"files": [
		"dist"
	],
	"scripts": {
		"build:dev": "tsc",
		"watch:dev": "tsc -w --preserveWatchOutput",
		"clean": "rimraf dist",
		"test": "node --test",
		"test:watch": "node --test --watch",
		"build": "npm run clean && tsc -p tsconfig.build.json"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.10.3",
		"rimraf": "^5.0.5",
		"typescript": "^5.3.2"
	}
}
